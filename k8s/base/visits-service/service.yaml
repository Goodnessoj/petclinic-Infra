apiVersion: v1
kind: Service
metadata:
  name: visits-service
  namespace: petclinic-dev
  labels:
    app: visits-service
spec:
  selector:
    app: visits-service
  ports:
  - port: 8082
    targetPort: 8082
    name: http
  type: ClusterIP