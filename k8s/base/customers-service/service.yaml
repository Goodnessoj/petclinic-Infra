apiVersion: v1
kind: Service
metadata:
  name: customers-service
  namespace: petclinic-dev
  labels:
    app: customers-service
spec:
  selector:
    app: customers-service
  ports:
  - port: 8081
    targetPort: 8081
    name: http
  type: ClusterIP