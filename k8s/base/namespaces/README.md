# Raw Namespace Manifests

This folder is reserved for raw namespace manifests.

The current YAML files are empty. The active application namespace is created by
the Terraform `addons` module and can also be created by Argo CD with
`CreateNamespace=true`.
